/**
 * Все числа, которые хочется крутить руками, живут здесь.
 * Правишь — сохраняешь — Vite сам перезагружает страницу.
 */

export const TUNING = {
  /** Горизонтальное движение */
  move: {
    walkSpeed: 260,      // обычный бег, px/сек
    accel: 2000,         // разгон
    decelGround: 2600,   // торможение на земле
    decelAir: 900,       // торможение в воздухе
    airControl: 0.75,    // насколько слушается управление в полёте (0..1)
    turnBoost: 1.6,      // множитель разгона при развороте — отзывчивее ощущается
  },

  /** Прыжок и гравитация */
  jump: {
    gravity: 2000,
    velocity: -800,        // первый прыжок, высота примерно 160 px
    doubleVelocity: -660,  // второй прыжок, чуть слабее
    maxJumps: 2,
    fallGravityMul: 1.45,  // падаем тяжелее, чем взлетаем — так прыжок «читается»
    cutMultiplier: 0.42,   // отпустил кнопку на взлёте — скорость режется
    coyoteMs: 110,         // сколько ещё можно прыгнуть, сбежав с края
    bufferMs: 130,         // нажал чуть раньше приземления — прыжок запомнится
    maxFallSpeed: 1200,
  },

  /** Рывок */
  dash: {
    speed: 640,
    durationMs: 170,
    cooldownMs: 520,
    ghostIntervalMs: 26,   // как часто оставляем полупрозрачную копию
    ghostLifeMs: 240,
    ghostAlpha: 0.4,
  },

  /**
   * Хитбокс — прямоугольник в мировых пикселях, отдельно от размера спрайта.
   * Фото по краям всегда шире реальной фигуры, поэтому эти числа меньше картинки.
   */
  hitbox: {
    width: 44,
    height: 108,
  },

  /** Как рисуем спрайт игрока */
  sprite: {
    scale: 0.34,       // кадр 340x460 -> фигура примерно 115 px ростом
    tiltMax: 8,        // максимальный наклон в полёте, градусов
    tiltPerSpeed: 0.012, // сколько градусов на единицу вертикальной скорости
    tiltLerp: 0.18,    // сглаживание наклона
  },

  /** Squash & stretch — держим в пределах 10-12%, иначе фото «поплывёт» */
  squash: {
    crouchMs: 90,        // приседание перед прыжком
    crouchScaleY: 0.92,
    crouchScaleX: 1.06,
    stretchScaleY: 1.10, // вытягивание на взлёте
    stretchScaleX: 0.93,
    landScaleY: 0.88,    // сплющивание при приземлении
    landScaleX: 1.12,
    landMs: 70,          // примерно 4 кадра при 60 fps
    recoverMs: 130,
  },

  /** Анимация */
  anim: {
    walkFpsMin: 8,
    walkFpsMax: 14,
    walkFpsSpeedRef: 260, // при какой скорости достигается максимальный fps
    breathScale: 0.015,   // «дыхание» в стойке, +-1.5% по Y
    breathBobY: 2,        // микро-покачивание по вертикали, px
    breathMs: 2000,
  },

  /** Контактная тень под персонажем */
  shadow: {
    radiusX: 30,
    radiusY: 9,
    alphaGround: 0.34,
    alphaAir: 0.12,
    airShrink: 0.55,     // во сколько раз тень ужимается высоко в воздухе
    heightFalloff: 220,  // на какой высоте тень становится минимальной
    color: 0x1b1430,
    offsetY: 2,
  },
};

export type Tuning = typeof TUNING;
